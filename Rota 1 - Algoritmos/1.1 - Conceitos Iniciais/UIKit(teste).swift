import UIKit //apenas é possivel utilizar raiz quadrada e potênciação com a classe UIkit.

var x, y, z, resultado: Double
x = 2
y = 5
z = 125

resultado = pow(x,y)
print("\(x) elevado a \(y) resulta em \(resultado)")
resultado = sqrt(z)
print("A raiz quadrada de \(z) é \(resultado)")